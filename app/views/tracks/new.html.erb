<% if @track.errors.any? %>
  <ul>
     <% @track.errors.full_messages.each do |error_msg| %>
      <li><%= error_msg %></li>
     <% end %>
  </ul>
<% end %>

<%= form_for @track do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit 'Create Track' %>
<% end %>
