<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <%= link_to user_path(current_user) do %>
            <span>
            <%= image_tag(current_user.picture.thumb,
                        :class => 'img-circle') %>
            </span>
            <span class="clear">
                <span class="block m-t-xs">
                    <strong class="font-bold"><%= current_user.name %></strong>
                </span> <span class="text-muted text-xs block">Web developer</span>
            </span>
            <% end %>
        </div>
        <div class="logo-element">
            SS
        </div>
      </li>
      <li class="<%= 'active' if current_page?('/') %>">
          <%= link_to "/" do %>
          <i class="fa fa-home"></i> <span class="nav-label">Home</span>
          <% end %>
      </li>
      <li class="<%= 'active' if current_page?('/tracks') %>">
          <%= link_to "/tracks" do %>
          <i class="fa fa-code-fork"></i> <span class="nav-label">Tracks</span>
          <% end %>
      </li>
      <li class="<%= 'active' if current_page?('/library') %>">
          <%= link_to "/library" do %>
          <i class="fa fa-university"></i> <span class="nav-label">Library</span>
          <% end %>
      </li>
      <li class="<%= 'active' if current_page?('/search') %>">
          <%= link_to "/search" do %>
          <i class="fa fa-search"></i> <span class="nav-label">Discover</span>
          <% end %>
      </li>
    </ul>
  </div>
</nav>
